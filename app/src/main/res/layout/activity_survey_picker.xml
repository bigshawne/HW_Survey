<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="edu.lawrence.hw_survey.SurveyPicker">

    <ListView
        android:id="@+id/survey_list"
        android:layout_width="342dp"
        android:layout_height="345dp"
        android:layout_marginBottom="128dp"
        android:layout_marginTop="24dp"
        android:listSelector="@android:color/holo_blue_light"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="1.0"
        tools:choiceMode="singleChoice" />

    <Button
        android:id="@+id/surveyReader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="148dp"
        android:layout_marginStart="148dp"
        android:layout_marginTop="52dp"
        android:onClick="readSurvey"
        android:text="@string/picker_botton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/survey_list" />
</android.support.constraint.ConstraintLayout>
